public class GeneralizedHarmonic {
	public static void main(String[] args) {
		int n = Integer.parseInt(args[0]);
		int r = Integer.parseInt(args[1]);
		double H = 0;
		for (int i = 1; i < n + 1; i++) {
			System.out.println("Fraction is " + (1/(Math.pow(i,r))));
			System.out.println("H is " + H);
			H  = ((1/(Math.pow(i,r))) + H);
			SH);
		}
	}
}
